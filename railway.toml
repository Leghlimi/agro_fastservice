[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py collectstatic --noinput && python manage.py migrate && gunicorn agro_fastservice.wsgi --log-file - --bind 0.0.0.0:$PORT"
healthcheckPath = "/"