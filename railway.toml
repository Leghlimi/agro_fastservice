  [build]
  builder = "nixpacks"
  buildCommand = "python manage.py collectstatic --noinput"

  [deploy]
  startCommand = "python manage.py migrate --fake contenttypes 0001 && python manage.py migrate && gunicorn agro_fastservice.wsgi --log-file - --bind 0.0.0.0:$PORT"
  healthcheckPath = "/"